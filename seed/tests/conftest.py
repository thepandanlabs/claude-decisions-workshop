# TODO: Test setup.
# - Patch the Claude API call in extract.py so tests never hit the real API
# - Provide a fixture that returns a temporary database path
# - Load any recorded extraction fixtures from tests/fixtures/ (add this dir when implementing)
