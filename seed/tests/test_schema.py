# TODO: Schema validation test.
# Confirm that extract() returns a list of dicts with all required fields
# from the PRD.md extracted record schema:
#   type, content, owner, deadline, tags, source_file, meeting_date
# Uses a recorded fixture — never calls the real Claude API.
