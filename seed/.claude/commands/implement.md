Execute the plan produced by /plan.

Rules:
- Work through the plan steps in order. Check off each step as you complete it.
- Do not skip steps or reorder them without explaining why.
- If you encounter a decision not covered by the plan, make the reasonable call and note it — do not stop and ask unless blocked.
- After each file is complete, run `python -c "import decisions"` (or the equivalent check) to confirm it imports without error.
- Do not move on to the verification step until all implementation steps are done.
